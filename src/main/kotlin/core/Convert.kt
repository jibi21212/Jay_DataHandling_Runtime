package core

// Should have a void method which returns the desired type converted easily!
// Should have prebuilt procedures to handle all regular conversions
// Methods for handling conversions between built in types should all be static methods (not requiring an instantiated object to be run
// Should allow for defining new ways of converting via creating original + ConvertTO + procedures to actually define conversion process
// Will need to instantiate an object in order to create custom conversions

enum class PrimType{
    STRING,
    INT,
    FLOAT,
    BOOL,
}
// Add toFloat + toBool functions, add flag handling
class Convert(){



}
